spring:
  application:
    name: config-server-service
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
config-server-service:
  greeting: Botafogo
  default-value: Campeão Brasileiro